services:
  myapp:
    build: .
    container_name: myapp
    restart: always
    ports:
      - "127.0.0.1:8001:8001"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3